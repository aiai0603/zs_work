#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../../../_ecstatic@3.3.2@ecstatic/lib/ecstatic.js" "$@"
  ret=$?
else 
  node  "$basedir/../../../_ecstatic@3.3.2@ecstatic/lib/ecstatic.js" "$@"
  ret=$?
fi
exit $ret
