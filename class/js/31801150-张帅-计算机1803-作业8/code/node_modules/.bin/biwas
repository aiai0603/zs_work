#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../_biwascheme@0.7.2@biwascheme/bin/biwas" "$@"
  ret=$?
else 
  node  "$basedir/../_biwascheme@0.7.2@biwascheme/bin/biwas" "$@"
  ret=$?
fi
exit $ret
